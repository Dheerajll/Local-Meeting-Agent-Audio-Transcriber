[project]
name = "local-meeting-agent"
version = "0.1.0"
description = "Local Meeting Agent"
requires-python = ">=3.11"

dependencies = [
    "mlx-whisper",
    "numpy",
    "torch",
    "silero-vad",
    "playwright",
    "huggingface_hub"
]

[project.scripts]
lma = "lma.cli:main"